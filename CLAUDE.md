# CLAUDE.md

Claude-specific entry point. It holds **no rules of its own** — by design.

- **Canonical laws (all AI vendors, all projects):** `Autocss-com/ai` → `AGENTS.md`.
  Delivered via your user-level `~/.claude/CLAUDE.md` (`@~/.claude/ai/AGENTS.md`), which
  Claude Code auto-loads for every project. Setup: see `Autocss-com/ai/AGENTS.md`.

Never restate canonical rules here. Edit them in `Autocss-com/ai`; every project follows.
